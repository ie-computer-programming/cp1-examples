# CMake generates everything in here; disposable, never commit it
build/
build-*/

# Compiled output
*.o
*.a
*.exe
*.out

# Editor / OS
# .vscode/* (not .vscode/) so the committed example below can be un-ignored
.vscode/*
!.vscode/settings.json.example
.DS_Store
